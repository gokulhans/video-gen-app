{
  "name": "ai-video-cf",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @app/api --filter @app/admin --filter @app/pipeline dev",
    "typecheck": "pnpm -r typecheck",
    "deploy": "pnpm -r deploy"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^5.20260708.1"
  },
  "pnpm": {
    "overrides": {
      "esbuild@<=0.24.2": "0.25.0"
    }
  }
}
