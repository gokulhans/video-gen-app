# Deploy Runbook

Moved: see [`../DEPLOY.md`](../DEPLOY.md) at the repo root — it reflects the
live deployment (resource IDs, secrets setup with commands, AI Gateway, CI
container build, Flutter env, smoke tests, data migration).
